## Harness: Cumuru Bootstrap

**Objetivo:** executar `prompts/BOOTSTRAP-CODEX.md` por fase, com estudo
paralelo, implementação controlada e QA incremental.

**Trigger:** para executar, continuar, retomar, reexecutar, atualizar ou auditar
uma fase do Cumuru, use o skill `cumuru-bootstrap`. Perguntas conceituais simples
podem ser respondidas diretamente.

**Completion gate:** toda tarefa que altera código e possui `CLAIM` só pode
emitir `DONE` ou handoff depois de `make post-task-quality` concluir com exit
code zero e o artifact anexar `POST_TASK_QUALITY=PASS`. O QA recusa ausência do
comando, resultado ou marcador.

**Histórico de mudanças:**

| Data | Mudança | Alvo | Motivo |
| --- | --- | --- | --- |
| 2026-08-20 | Ritmo vertical dos formulários e classes usadas sem CSS | `apps/web/src/styles.css`, `apps/web/src/landing.css`, `apps/web/src/features/accommodation/PublicListingPanel.tsx`, `apps/web/src/features/analytics/external-context/ExternalContextTab.tsx` | Formulário sem classe própria dentro de cartão ou painel não declarava `gap`, e os blocos que ele empilha zeram a própria margem: campo, caixa de marcar, erro e botão encostavam com zero pixel entre si. A auditoria cruzando as classes usadas no JSX com as definidas na folha achou a mesma causa em mais lugares — `.form-error`, `.analytics-layers` e a família `external-*` inteira eram usadas e nunca definidas, então a aba de contexto externo renderizava como HTML cru, e `var(--ink-soft)` era token inexistente. A fita do card externo é neutra por decisão, não por falta: a camada não é medida nossa, nem observada nem prevista. Só CSS e markup; sem migration e sem escopo novo |
| 2026-08-19 | Leituras novas dentro do privilégio de `app_runtime` | `apps/api/internal/platform/store/queries/own_performance.sql`, `.../funnel.sql`, `contracts/openapi.yaml` | O comparativo lia `analytics.presence_days` e o funil lia `survey.responses`; `app_runtime` não tem SELECT em nenhuma das duas — a primeira é tabela do worker, e na segunda a API grava e não lê de volta. A e2e acusou com 15 respostas 503, e a verificação anterior contra PostgreSQL real não pegou porque rodou como superusuário. As pessoas-dia passam a ser derivadas de `core.stays` e `core.visitors`, repetindo `presenceEligible`, e a conclusão da pesquisa passa a sair de `capabilities.consumed_at`. O preço fica declarado: sem ler `participation`, o funil não separa resposta de recusa — essa separação só pode vir do worker |
| 2026-08-19 | Guia da administração para o dia a dia | `docs/guias/guia-administracao-dia-a-dia.md` | As cinco tarefas semanais da administração existiam só como conhecimento tácito, e duas delas são irreversíveis: recusar pedido apaga o contato na mesma transação (ADR-042), e a ativação é de uso único (ADR-041). Sem critério escrito, decisão passa a depender de quem está de plantão. O guia também registra a lacuna do direito de titular — não há tela, o caderno é o controle — em vez de omiti-la. Só documentação |
| 2026-08-19 | Material de campo do piloto em linguagem simples | `docs/guias/guia-pousada-como-pedir-o-cadastro.md`, `docs/guias/guia-piloto-30-dias.md`, README | O sistema já emite o cartaz com QR, mas nada dizia onde colar, o que escrever ao lado nem o que falar ao hóspede — e é aí que a adesão se ganha ou se perde, não no código. O guia da hospedagem põe o cartaz junto da senha do Wi-Fi, que é o único ponto por onde todo hóspede passa, e traz a frase da saída, quando a pessoa tem tempo e memória da estadia. O guia do piloto divide as hospedagens em dois grupos por duas semanas: sem grupo de comparação, uma alta de conversão não se distingue de sazonalidade ou novidade. Os dois recusam brinde e sorteio pelo mesmo motivo técnico — resposta comprada enviesa a distribuição publicada. Só documentação |
| 2026-08-19 | Funil de adesão medido sem instrumentar ninguém | `apps/api/internal/analytics/funnel.go`, `apps/api/internal/platform/store/queries/funnel.sql`, `contracts/openapi.yaml`, `apps/web/src/features/analytics/AnalyticsQuality.tsx` | Não dava para saber onde a adesão se perde: o painel media qualidade do dado, não conversão do fluxo. O funil sai de estados que convites, capabilities e aprovações já guardam, então não há telemetria, evento por pessoa nem base legal nova a discutir. A mediana de latência tem piso de dez submissões — com uma submissão na janela a "mediana" é o tempo exato de um hóspede. Diagnóstico interno: não entra em publicação. Sem migration e sem escopo novo |
| 2026-08-19 | Ocupação da janela atrás de autenticação, não como célula pública | `apps/api/internal/analytics/occupancy.go`, `apps/api/internal/platform/store/queries/own_performance.sql`, `contracts/openapi.yaml`, `apps/web/src/features/performance` | A ocupação da vila era o número mais pedido e não existia. Publicá-la como família nova, porém, equivale a publicar a capacidade da vila: com presença e cobertura já públicas, `capacidade ≈ presença / ocupação`, e a janela de 730 dias dá observações suficientes para a média convergir apesar do arredondamento — a diferença entre publicações denunciaria a capacidade do estabelecimento admitido. Serve pelo endpoint autenticado, um número por janela em banda de cinco pontos, com os guardas da Camada A. Sem migration e sem escopo novo |
| 2026-08-19 | Comparativo da hospedagem contra a série publicada | `apps/api/internal/analytics/own*.go`, `apps/api/internal/platform/store/own_performance_repository.go`, `contracts/openapi.yaml`, `apps/web/src/features/performance` | A área da hospedagem só pedia: quadro de estadias, cadastro e fila de hóspedes, nenhum retorno do dado que ela digita. O painel devolve a curva própria ao lado do índice da vila, sem publicar família de célula nova — o lado agregado é a publicação corrente, lida pela mesma role restrita da capa. O limiar do comparativo é mais alto que o público (cinco reportantes, não três) porque quem lê sabe o próprio número e só ela consegue subtrair-se do total; acima de um quarto do denominador o agregado vira espelho e a comparação fecha. Sem migration e sem escopo novo: `stays:read:own`, que já libera a listagem de estadias, mais sensível |
| 2026-08-19 | Questionários fora da área da hospedagem | `apps/api/internal/platform/localdemo/fixtures.go` | A conta fictícia de operação carregava `questionnaires:manage` e `questionnaires:approve`, e o item `/questionários` é gateado por esse escopo no cliente: quem apenas opera uma hospedagem enxergava a administração do catálogo. O catálogo é global — `questionnaire.Service` e seu store não cruzam `core.memberships` —, então o escopo edita, aprova, publica e aposenta a versão que a plataforma inteira responde. Mesma classe do `accommodations:onboard` retirado em 2026-08-18. A publicação da fixture não depende da conta: usa `fixture-questionnaire-editor` e `-reviewer`. Sem migration e sem escopo novo |
| 2026-08-19 | Importação do calendário do Booking.com | `apps/api/internal/calendarfeed`, `apps/api/migrations/000004`, `apps/api/internal/platform/store/calendar_*`, `apps/web/src/features/calendar`, contrato, ADR-044 | A hospedagem digitava chegada e saída duas vezes: uma no extranet do Booking, outra aqui. A API do Booking está fechada para quem nós somos — o Connectivity exige parceiro homologado que gerencia tarifa e disponibilidade —, então a origem é o `.ics` que o próprio anfitrião exporta. Ele não traz identidade, não traz número de hóspedes e não separa reserva de bloqueio de manutenção com confiabilidade, e por isso o import gera reserva importada e nunca estadia: sem confirmação humana, um bloqueio de manutenção viraria presença publicada. URL cifrada com keyring próprio, UID sob HMAC, atrás de `CALENDAR_FEED_ENABLED` |
| 2026-08-18 | Lista pública de hospedagens com telefone | `apps/api/migrations/000003`, `apps/api/internal/directory`, `apps/api/internal/platform/httpapi/directory.go`, `apps/web/src/features/directory`, `apps/web/src/features/accommodation/PublicListingPanel.tsx`, contrato, ADR-043 | O hóspede que chega na praia não tinha por onde descobrir quem hospeda: o cadastro só é legível por dentro, e a capa publica estatística agregada, que nunca responde "para quem eu ligo". `core.accommodations` ganha contato publicado e consentimento vivo; a lista lê o cadastro, não `public_data`, porque despublicar precisa sumir na mesma transação e contato consentido não é amostra sujeita a supressão. Segunda exceção nominal à ADR-040, no limite da ADR-042: só se publica quem se descreve a si mesmo |
| 2026-08-18 | Asserções de `lint-shell` e do gate de CI apontadas para o alvo real | `.agents/skills/cumuru-bootstrap/scripts/harness.sh` | `make harness-validate` estava vermelho antes da onda 8, por duas asserções pinadas a uma forma de escrita que migrou. `validate_completion_contract` exigia o corpo inline de `lint-shell` no Makefile, mas a lógica foi para `deploy/scripts/lint-shell.sh` e trocou `find` por `git ls-files`, de propósito, para ignorar caminhos gitignored; e a asserção do `ci.yml` exigia `make post-task-quality` em linha isolada, forma que virou `run: make post-task-quality`, o que deixava o `grep` negativo de `make lint` fail-open. As duas passaram a ler o alvo atual. `SHELL_LINT_FAIL_OPEN_DRIFT` foi preservado e agora varre também o script; `CI_QUALITY_DRIFT` voltou a morder |
| 2026-08-18 | Camada de contexto externo com papel próprio de ingestão | `apps/api/migrations/000005_external_context`, `database/schema.sql`, `contracts/openapi.yaml`, `apps/api/internal/platform/store/queries`, `deploy/postgres/init`, `deploy/ansible`, `deploy/scripts/test-migrations.sh`, `test-local-restore.sh`, docs/03, docs/04 | Dado de terceiro passou a aparecer ao lado da série que a plataforma mede, e sem fronteira aplicada pelo banco a frase da ADR-029 — "não chama fonte externa" — viraria afirmação não verificável. `external` entra como schema próprio porque quatro CHECKs de `000001`/`000002` recusam o dado externo e afrouxá-los apagaria a evidência do método publicado. A ingestão ganha papel dedicado `external_runtime`: reusar `worker_runtime` daria à camada externa a leitura de `core`/`survey`/`analytics` e tornaria a asserção negativa de ACL impossível de passar, já que ele lê os quatro schemas hoje. As views novas moram em `public_data` e lê `external` sob privilégio do dono, então `public_pool.go` e a CSP não mudam. Quebra a contagem de views e de schemas nos scripts de migration e de restauração |
| 2026-08-18 | Área da administração separada da área da hospedagem | `apps/web/src/pages/AuthenticatedPage.tsx`, `apps/web/src/features/admin`, `apps/web/src/features/operator`, `apps/api/internal/platform/seed/run.go`, `deploy/e2e/local-demo.spec.ts`, README, CHANGELOG | O administrador entrava e via "Suas hospedagens", com quadro de estadias e fila de hóspedes, como se operasse os lugares que apenas admitiu — o vínculo existe porque entregar o acesso lê a linha e escreve a ativação, não porque a conta opere. `/acesso` passa a escolher a área por `accommodations:onboard`, o que a API tem no lugar de um papel, e o seed deixou de conceder `stays:write` ao administrador. Sem migration e sem escopo novo |
| 2026-08-18 | Fixtures de teste com vila inteira em operação | `apps/api/internal/platform/localdemo`, `apps/api/internal/platform/application/worker.go`, `deploy/scripts/test-local-demo.sh`, README, CHANGELOG | O perfil `test` semeava 4 hospedagens e 8 semanas de estadias de uma noite, então as janelas de 90, 365 e 730 dias, o seletor de mês e a cobertura por categoria abriam vazios e não dava para ver o produto funcionando. O catálogo passou a ter 16 hospedagens, com pelo menos três por categoria, e o calendário cobre os 730 dias da janela publicada com sazonalidade, grupos dimensionados pela capacidade e respostas parciais. O agendamento é ancorado no calendário civil, não na execução, para continuar idempotente; a reconciliação de analytics — no seed e no worker — e a espera no lock passaram a ter orçamento de lote, porque `DATABASE_TIMEOUT` é de requisição e todo ciclo do worker falhava no relógio com dois anos de dados |
| 2026-08-18 | Identidade própria para o token de desenvolvimento | `apps/api/internal/access`, `apps/api/internal/platform/localdemo`, `deploy/scripts/test-local-demo.sh`, `apps/api/README.md` | O token estático `cumuru-local-platform-read` devolvia o sujeito `fixture-operator`, o mesmo da operadora fictícia do `local-demo`; na trilha de auditoria a sondagem e a pessoa viravam o mesmo ator (emissor `https://oidc.invalid/local`, sujeito `fixture-operator`). O token passa a responder por `fixture-platform-probe` (`access.DevelopmentPlatformSubject`) e as memberships da trilha OIDC fictícia passam a ser dele, porque é o único que entra por aquele emissor; a operadora humana continua na trilha de sessão (`access.LocalSessionIssuer` + id da conta). Banco local já semeado quebra o próximo `local-demo` com conflito de fixture: rode `make docker-rm` antes |
| 2026-08-18 | Hierarquia administrador, estabelecimento e hóspede | `apps/api/internal/platform/localdemo/fixtures.go`, `apps/api/internal/access/access.go`, `apps/api/internal/platform/seed/run.go`, `apps/web/src/features/operator`, `deploy/e2e/local-demo.spec.ts`, `deploy/scripts/test-self-service-full-stack.sh` | Administrador e operador semeados eram indistinguíveis ao logar: ambos carregavam `accommodations:onboard`, que gateia `POST /accommodations` e as três rotas de decisão da fila do ADR-042, cuja listagem é global. O operador fictício podia admitir estabelecimento na plataforma inteira. O escopo saiu do operador e do token `cumuru-local-platform-read`, que o reabria por bearer no mesmo runtime (o verificador encadeado aceita as duas credenciais). Sem migration e sem escopo novo: admitir estabelecimento é ato do administrador, aprovar hóspede é ato do estabelecimento |
| 2026-08-18 | Pedido de convite da hospedagem | `core.accommodation_access_requests`, `apps/api/internal/accessrequest`, `apps/web` (`/convite` e fila do admin), contrato, ADR-042 | A capa convidava a pousada a se cadastrar e mandava quem clicava para `/acesso`, que é login e só abre por dentro. O canal público passa a gravar um pedido nominal, a administração aprova e a acomodação é criada pelo mesmo caminho do cadastro à mão. É a exceção declarada à ADR-040, estreita: identidade em canal aberto só de quem se descreve a si mesmo. Recusa e expiração eliminam o contato |
| 2026-08-18 | Janela histórica de dois anos na publicação pública | `contracts/openapi.yaml`, `apps/api/migrations/000002`, `apps/api/internal/analytics`, `apps/web/src/features/analytics` | O seletor de armazenamento era a janela de leitura, então só existiam trinta dias de histórico e cada janela nova duplicaria a mesma célula. `observed_daily` passa a guardar 730 dias e a janela vira recorte: quebra `recent_30_days` como `period_selector` e acrescenta `recent_90_days`, `recent_365_days`, `recent_730_days` e `month=YYYY-MM` ao contrato |
| 2026-08-18 | Administrador incondicional; operador atrás do perfil `test` | `compose.local.yaml`, `Makefile`, `deploy/scripts/test-analytics-full-stack.sh`, `deploy/scripts/test-local-demo-e2e.sh`, `deploy/scripts/test-self-service-full-stack.sh`, `README.md`, `deploy/README.md` | `make up`/`make docker-dev` deixavam o administrador atrás de `make seed` (profile `seed`, sob demanda) enquanto o operador fictício subia sempre pelo `local-demo`; sem administrador, uma stack recém-subida não tem por onde entrar. `seed` virou incondicional e fixa `SEED_PROFILE=admin`; `local-demo` foi para o perfil `test`, aplicado por `make seed-test-fixtures`/`make docker-dev-seed-test-fixtures` ou pelos scripts que precisam dele |
| 2026-08-17 | Terceira onda de consolidação da baseline de migrations | `apps/api/migrations`, `deploy/scripts/test-migrations.sh`, `deploy/scripts/test-self-service-integration.sh`, ADR-032 | Nenhum banco persistente foi montado ou lançado; `000002_organization_document`, `000003_self_service_and_approval`, `000004_rename_fnrh_failures_reason` e `000005_audit_outbox_returning_grants` voltaram a ser absorvidos em `000001_initial_schema` por concatenação literal, sem omitir nenhum bloco |
| 2026-08-17 | Código renomeado por funcionalidade | `apps/api`, `apps/web`, `contracts`, `deploy`, `Makefile`, CI, docs | Nomes como `Phase2Config` e `phase4-client.ts` obrigavam o leitor a conhecer a ordem de entrega para entender o código. Fase passou a existir só no harness (`BOOTSTRAP-CODEX`, `phase-matrix`), e código, config, contrato e alvos de teste falam `core`, `questionnaire`, `analytics` e `self-service`. Quebra `PHASE*_ENABLED`, `x-cumuru-implementation-phase` e o enum `phase_not_implemented` |
| 2026-08-16 | Convenção shfmt declarada | `.editorconfig` e scripts do harness | `shfmt` passou a existir no ambiente e assumia tabs contra os 2 espaços do repositório; `SHFMT` saiu de `UNVERIFIED` para `PASS` sem suprimir regra |
| 2026-08-16 | Fase 7 reescrita após o estudo | `prompts/BOOTSTRAP-CODEX.md`, `phase-matrix.md`, `harness.sh`, `test-harness.sh`, ADR-039/040/041 | O estudo provou que `identity.visitor_identities` não existe na cadeia executável (ADR-020 a vetou); o canal aberto passa a coletar só dado generalizado e a identidade vem depois pelo convite nominal da Fase 2. Gate renomeado para `SELF_SERVICE_LEGAL_BASIS`, porque o nome anterior descrevia algo que o desenho não faz mais |
| 2026-08-16 | Wave Fase 7 de autoatendimento e aprovação | `prompts/BOOTSTRAP-CODEX.md`, `phase-matrix.md`, `harness.sh`, `test-harness.sh`, `trigger-evals.md`, `Makefile`, `deploy/scripts/test-phase7-*.sh` | Cadastrar acomodação com ativação por capability, convite reutilizável por acomodação, autocadastro e aprovação do estabelecimento |
| 2026-08-16 | Correção do gate ShellCheck | `.agents/skills/cumuru-bootstrap/scripts/harness.sh` e `test-harness.sh` | ShellCheck 0.11 passou a acusar SC1007/SC2016 e quebrou `make harness-validate`; `CDPATH=''` e `$'...'` restauram o gate sem suprimir regra |
| 2026-07-29 | Protocolo e build de remediação | bootstrap e Fase 4 | Transformar débitos/runtime divergente em ondas reproduzíveis com PostgreSQL, full-stack e navegador real |
| 2026-07-28 | Gate global pós-tarefa | harness, agents e CI | Impedir handoff com complexidade/lint ausentes ou mascarados |
| 2026-07-28 | Configuração inicial | harness completo | Execução faseada no Codex com compatibilidade Claude |
