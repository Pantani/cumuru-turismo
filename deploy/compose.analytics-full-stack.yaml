name: cumuru-analytics-full-stack

services:
  postgres:
    ports: !override
      []
  api:
    environment:
      ANALYTICS_ENABLED: "true"
      PUBLIC_DATABASE_URL: postgres://cumuru_public:cumuru-local-public-only@postgres:5432/cumuru?sslmode=disable
      TRUSTED_PROXY_CIDRS: 172.30.${CUMURU_SUBNET_OCTET:-5}.10/32
  worker:
    environment:
      ANALYTICS_ENABLED: "true"
      ANALYTICS_INCREMENTAL_INTERVAL: 500ms
  web:
    ports: !override
      []
    networks:
      private:
        ipv4_address: 172.30.${CUMURU_SUBNET_OCTET:-5}.10

networks:
  private:
    ipam:
      config: !override
        - subnet: 172.30.${CUMURU_SUBNET_OCTET:-5}.0/24
